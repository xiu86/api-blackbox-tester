interface:
  display_name: "测试用例评审"
  short_description: "四角色评审测试用例并决定是否允许执行"
  default_prompt: "使用 $api-blackbox-test-reviewer 评审 planner 输出的测试用例，保存到 tests/【需求】_YYYYMMDD/测试用例评审.md；通过后再进入 executor，不通过则退回 planner 完善。"
policy:
  allow_implicit_invocation: true
